# Use an authenticated provider URL for sustained indexing workloads.
ETH_RPC_URL=https://ethereum.example/v1/replace-me

# The Compose application service publishes this host port.
HTTP_PORT=8080

# Optional and disabled by default. Only loopback listeners are accepted.
# PPROF_ADDRESS=127.0.0.1:6060
